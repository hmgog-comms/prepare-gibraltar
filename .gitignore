# Build output
_site/

# Dependencies
node_modules/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude
.claude/

# Environment
.env

# Logs
*.log
sndo-fillable-drafts/

# IBM Equal Access (achecker) scan output, and the URL list `npm run review` hands it
results/
.review/

# A PDF the generator was printing when it was interrupted — never output, never committed
src/assets/downloads/.*.pdf.tmp

# Wrangler local state
.wrangler/

# Internal working notes — never published
NOTES-INTERNAL.md

# CMS bundle: copied from node_modules at build time, not committed
admin/decap-cms.js

# Internal docs — kept locally, not published
docs-internal/
# Netlify CLI working state — created by `netlify deploy`, never committed
.netlify/

unverified.json
check-stderr.txt
